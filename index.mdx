---
title: "Define and generate pipelines"
description: "Generate Docker Buildx Bake plans from declarative build metadata and OCI-hosted charts."
---

`buildcharts` is a CLI tool for defining build intent in `build.yml` and turning that metadata into a Docker Buildx Bake plan. You keep build definitions in your repo, pull reusable chart templates from an OCI registry, and run the same generated plan locally or in CI.

It sits between handwritten pipeline YAML and raw Dockerfiles. You declare what should happen, BuildCharts resolves the chart-backed implementation, and Docker Buildx Bake executes the generated plan.

<CardGroup cols={2}>
  <Card title="Install" icon="download" href="/install">
    Install the BuildCharts CLI globally or in a local tool manifest.
  </Card>
  <Card title=".NET quickstart" icon="rocket" href="/quickstart">
    Scaffold a .NET repository and generate your first Bake plan.
  </Card>
  <Card title="CLI reference" icon="terminal" href="/reference/cli">
    Every command, option, and flag documented.
  </Card>
  <Card title="How it works" icon="gears" href="/guides/how-it-works">
    Understand the generation flow from metadata to Docker Bake.
  </Card>
</CardGroup>

## The model

BuildCharts is designed for teams that want to standardize builds across many repositories without locking themselves into one CI provider:

- **Intent** stays in repository metadata (`build.yml`)
- **Implementation** lives in versioned OCI charts (`Chart.yaml`)
- **Execution** runs through Docker Buildx Bake

That gives you a portable build layer that can be debugged locally and reused across repositories.

## What problem it solves

BuildCharts reduces common CI/CD problems:

- Duplicated or nested pipeline YAML across many repositories
- Platform-specific pipeline logic that is hard to move between providers
- Fragile template rollouts where repositories drift onto different versions
- Poor local debuggability where troubleshooting requires repeated CI runs

Instead of copying CI steps repo by repo, teams centralize the build implementation in charts and let repositories consume versioned updates through normal dependency changes.

## What you define

```text
src/
├── build.yml            # Build metadata
├── charts/
│   └── buildcharts/
│       └── Chart.yaml   # Chart dependency data
```

## What BuildCharts generates

```text
.buildcharts/
└── docker-bake.hcl      # Generated Docker Buildx Bake plan
```

## Workflow

<Steps>
  <Step title="Create or scaffold build.yml and Chart.yaml">
    Run `buildcharts init` in a .NET repository to scaffold both files automatically, or write them manually.
  </Step>
  <Step title="Pin chart versions (optional)">
    Run `buildcharts update` to resolve chart digests and write `Chart.lock` for reproducible builds.
  </Step>
  <Step title="Generate the Bake plan">
    Run `buildcharts generate` to validate metadata, pull charts, run plugins, and write `.buildcharts/docker-bake.hcl`.
  </Step>
  <Step title="Execute the plan">
    Run `docker buildx bake --file .buildcharts/docker-bake.hcl` to build with the generated plan.
  </Step>
</Steps>

## Why use it

- Keep CI logic in versioned OCI charts instead of copying pipeline YAML across repos
- Run the same generated build plan locally and in CI
- Keep repository config lightweight by separating intent in `build.yml` from implementation in charts
- Stay provider-agnostic by generating Docker-native build plans instead of provider-specific steps
- Adopt chart updates gradually with versioned dependencies and optional lock files
- Describe intent with aliases such as `build`, `test`, `nuget`, and `docker`
- Extend generation with built-in plugins

## Explore the docs

<CardGroup cols={2}>
  <Card title="Core features" icon="star" href="/core-features/scaffolding">
    Scaffolding, metadata, generation, lock files, plugins, and charts.
  </Card>
  <Card title="How-to guides" icon="book" href="/how-to/define-build-stages">
    Define build stages, use outputs, and handle dependencies.
  </Card>
  <Card title="Plugins" icon="puzzle-piece" href="/core-features/plugins">
    NuGetAuthenticate and TestcontainersDinD built-in plugins.
  </Card>
  <Card title="GitHub Actions" icon="github" href="/guides/github-actions">
    Run BuildCharts in your GitHub Actions workflows.
  </Card>
</CardGroup>
